function sendHelloWorldToDiscord() {
  var discordWebhookUrl = "link";
  var message = "Hello, World! This is a message from Google Apps Script.";

  var payload = {
    "content": message,
  };

  var options = {
    "method": "post",
    "payload": JSON.stringify(payload),
    "contentType": "application/json",
  };

  UrlFetchApp.fetch(discordWebhookUrl, options);
